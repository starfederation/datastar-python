[tool.ruff]
line-length = 99
lint.select = [
  # flake8-bugbear
  "B",
  # pycodestyle
  "E",
  # Pyflakes
  "F",
  # isort
  "I",
  # flake8-simplify
  "SIM",
  # pyupgrade
  "UP",
]
lint.extend-ignore = [
  "E501", # line too long
]
lint.fixable = [ "ALL" ]
